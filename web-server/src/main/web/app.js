const React = require('react');
const ReactDOM = require('react-dom');

class App extends React.Component {
    render() {
        return (
            <div>LET US ACTUALLY COMMENCE</div>
        );
    }
}

ReactDOM.render(
	<App />,
	document.getElementById('react')
)
